import { ReputationModel } from '../types';

/**
 * Function to sort the sentiment analysis data based on its date
 * note: Setting this just a placeholder function, assuming that the backend will sort the data.
 * @param model Reputation model from the backend
 * @returns
 */
export const sort = (model: ReputationModel) => ({
  ...model,
});
